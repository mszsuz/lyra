# Архитектура Lyra-Chat-multiform.epf

> Модульная версия обработки Lyra-Chat. Монолитная форма (1521 строка) разнесена по служебным формам-модулям.
> Паттерны взяты из анализа трёх production-обработок: СБИС (131 форма), Диадок (56 форм), Vanessa (108 форм).

---

## Содержание

**Реализовано:**
1. [Целевая архитектура](#1-целевая-архитектура) — исходная vs реализованная структура
2. [Разбиение на модули](#2-разбиение-на-модули) — контракт модуля, DI, события
3. [Шаблоны форм-модулей](#3-шаблоны-форм-модулей) — Bridge, Парсер, MCP, Чат
4. [Жизненный цикл и инициализация](#4-жизненный-цикл-и-инициализация) — серверная/клиентская, каскадное отключение
5. [Ограничения платформы](#5-ограничения-платформы) — `&НаКлиентеНаСервереБезКонтекста`, ISP, пинг-понг

**Паттерны и рекомендации:**
6. [WebSocket и Bridge](#6-websocket-и-bridge) — retry, настройки
7. [Парсинг JSON](#7-парсинг-json) ✅ — ЧтениеJSON + Соответствие
8. [MCP-инструменты](#8-mcp-инструменты) ✅ — Strategy-dispatch
9. [Кэширование и состояние](#9-кэширование-и-состояние)
10. [Настройки и хранилища](#10-настройки-и-хранилища) ✅ — обёртки ObjectModule
11. [Обработка ошибок](#11-обработка-ошибок)
12. [UI и динамические формы](#12-ui-и-динамические-формы)
13. [Совместимость](#13-совместимость) ✅ — динамическое имя
14. [Тестируемость](#14-тестируемость)
15. [Статус реализации](#15-статус-реализации) — что сделано, что осталось
16. [Как использовать](#16-как-использовать)

---

## 1. Целевая архитектура

### Исходная версия (Lyra-Chat.epf)

```
Lyra-Chat.epf
├── Форма (1521 строка — god-object)
│   ├── UI-обработчики
│   ├── WebSocket/Bridge
│   ├── Парсинг JSON (строковый, хрупкий)
│   ├── MCP-инструменты (цепочка Если/ИначеЕсли)
│   ├── Вывод в чат
│   └── Копипаста ЕХТ_Чат (440 строк)
├── ФормаПодключение (пустая)
├── ObjectModule (пустой)
└── 2 макета
```

### Реализованная архитектура (Lyra-Chat-multiform.epf)

```
Lyra-Chat-multiform.epf
├── Форма (главная, 854 строки)
│   ├── Тонкий контроллер — UI-события и координация модулей
│   ├── ОбработатьСобытиеМодуля() — центральный диспетчер событий
│   ├── Обработчики настроек чата (аватары, автосвертка, тёмная тема)
│   └── Встроенный код фреймворка ЕХТ_Чат (без изменений)
├── МодульBridge (174 строки) — WebSocket-соединение
├── МодульПарсер (187 строк) — JSON через ЧтениеJSON в Соответствие
├── МодульMCP (335 строк) — Strategy-dispatch + серверные обработчики
├── ФормаПодключение (48 строк) — UI настроек (URL, модель, промпт)
├── ObjectModule (54 строки) — обёртки настроек, ПолноеИмя()
└── 2 макета (HTML/JS чата, копия из оригинала)
```

**Источник паттерна:** Vanessa — главная форма хранит ссылки на модули в `Перем`, инициализирует их в `ПриОткрытии`, каскадно отключает в `ПередЗакрытием`.

**Отличие от плана:** МодульЧат не создан — код ЕХТ_Чат остался в главной форме. Причина: ЕХТ_Чат тесно привязан к элементам формы (Чат1) и плохо выносится в отдельный модуль без переписывания фреймворка.

### Структура файлов

```
src/
├── Lyra-Chat-multiform.xml                         # Корневой XML (5 форм, 2 макета)
└── Lyra-Chat-multiform/
    ├── Ext/
    │   └── ObjectModule.bsl                         # 54 строки — обёртки настроек, ПолноеИмя()
    ├── Forms/
    │   ├── Форма.xml                                # Метаданные главной формы
    │   ├── Форма/Ext/
    │   │   ├── Form.xml                             # UI-разметка (копия из оригинала)
    │   │   └── Form/Module.bsl                      # 854 строки — тонкий контроллер + ЕХТ_Чат
    │   ├── МодульBridge.xml
    │   ├── МодульBridge/Ext/
    │   │   ├── Form.xml                             # Минимальная служебная форма
    │   │   └── Form/Module.bsl                      # 174 строки — WebSocket-соединение
    │   ├── МодульПарсер.xml
    │   ├── МодульПарсер/Ext/
    │   │   ├── Form.xml
    │   │   └── Form/Module.bsl                      # 186 строк — JSON через ЧтениеJSON
    │   ├── МодульMCP.xml
    │   ├── МодульMCP/Ext/
    │   │   ├── Form.xml
    │   │   └── Form/Module.bsl                      # 335 строк — MCP strategy-dispatch
    │   ├── ФормаПодключение.xml
    │   └── ФормаПодключение/Ext/
    │       ├── Form.xml                             # Форма настроек подключения
    │       └── Form/Module.bsl                      # 48 строк — URL, модель, промпт
    └── Templates/
        ├── ДвоичныеДанныеПриложения.xml
        ├── ДвоичныеДанныеПриложения/Ext/Template.bin    # 463 КБ — HTML/JS чата
        ├── ЕХТ_Чат_ДвоичныеДанныеПриложения.xml
        └── ЕХТ_Чат_ДвоичныеДанныеПриложения/Ext/Template.bin
```

### Сравнение с оригиналом

| | Lyra-Chat.epf | Lyra-Chat-multiform.epf |
|---|---|---|
| Формы | 2 (Форма + пустая ФормаПодключение) | 5 (Форма + 3 модуля + ФормаПодключение) |
| Главная форма | 1521 строка (God-object) | 854 строки (контроллер + ЕХТ_Чат) |
| JSON-парсинг | Строковый поиск через `СтрНайти` | `ЧтениеJSON` в Соответствие |
| MCP-dispatch | `Если/ИначеЕсли` цепочка | Реестр `Соответствие` (strategy) |
| WebSocket | Встроен в главную форму | Отдельный МодульBridge |
| Расширяемость | Только редактирование монолита | Добавление обработчиков через `ЗарегистрироватьОбработчик()` |

---

## 2. Разбиение на модули

### 2.1 Реализованное разбиение

| Модуль | Строк | Что содержит | Статус |
|---|---|---|---|
| **МодульBridge** | 174 | WebSocket-соединение, отправка, закрытие, обработчики WS_* | ✅ Реализован |
| **МодульПарсер** | 187 | `РазобратьJSON`, `ИзвлечьТекстДельты`, `СобратьJSON`, `ЭкранироватьJSON` | ✅ Реализован |
| **МодульMCP** | 335 | Strategy-dispatch, серверные обработчики `1c_query/metadata/eval/exec` | ✅ Реализован |
| **ФормаПодключение** | 48 | URL Bridge, выбор модели, системный промпт | ✅ Реализован |
| **МодульЧат** | — | Вывод в чат, стриминг | ❌ Не создан (ЕХТ_Чат остался в главной форме) |

**Взаимодействие модулей:**

```
Форма (контроллер)
  ├── ПолучитьФорму("МодульBridge")   → Bridge
  ├── ПолучитьФорму("МодульПарсер")   → Парсер
  ├── ПолучитьФорму("МодульMCP")      → MCP
  └── ОткрытьФорму("ФормаПодключение") → UI настроек
```

**Поток данных:**

```
1. Пользователь вводит текст
   → Форма.Чат1_ОтправитьСообщение
   → МодульПарсер.СобратьJSON (формирование {"type":"chat","content":"..."})
   → МодульBridge.ОтправитьСообщение (WebSocket)

2. Bridge отвечает
   → МодульBridge.WS_ПриПолученииСообщения
   → Владелец.ОбработатьСобытиеМодуля("Bridge", "bridge_message", JSON)

3. Форма обрабатывает ответ
   → МодульПарсер.РазобратьJSON (ЧтениеJSON → Соответствие)
   → обработка по типу события (session, stream_event, result, error, mcp_request...)

4. MCP-запрос
   → МодульMCP.ОбработатьЗапрос (strategy-dispatch по имени инструмента)
   → серверный обработчик (запрос/метаданные/вычисление/выполнение)
   → МодульBridge.ОтправитьСообщение (JSON-ответ)
```

### 2.2 Контракт модуля (из Vanessa)

Каждый модуль реализует минимальный контракт:

```1c
// Обязательно:
&НаКлиенте
Процедура Инициализация(Владелец) Экспорт    // DI — явная передача зависимости
&НаКлиенте
Функция ОтключениеМодуля() Экспорт

// Ссылка на владельца:
&НаКлиенте
Перем Владелец;  // главная форма, через которую модуль отправляет события
```

**Источник паттерна:** Vanessa — `ИнициализацияФормы(ВладелецФормы)` / `ОтключениеМодуля()` у каждого модуля.

**DI вместо Service Locator:** Зависимости передаются явно через `Инициализация(Владелец)`, а не через глобальный кеш. Это даёт понятный контракт модуля и отсутствие скрытых связей.

**Обратная связь через события:** Модули вызывают `Владелец.ОбработатьСобытиеМодуля("Bridge", "bridge_message", Данные)` — экспортный метод главной формы. Главная форма — единственный диспетчер событий.

**ВАЖНО: все экспортные методы — `&НаКлиенте`.** Методы с `&НаКлиентеНаСервереБезКонтекста` недоступны через `ПолучитьФорму()` (см. раздел 5).

### 2.3 Главная форма (реализованная структура)

```1c
// Форма/Ext/Form/Module.bsl — 854 строки (контроллер + ЕХТ_Чат)

// === Объявление модулей ===
&НаКлиенте
Перем МодульBridge;
&НаКлиенте
Перем МодульПарсер;
&НаКлиенте
Перем МодульMCP;

// === Инициализация ===
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    ПутьКФормам = РеквизитФормыВЗначение("Объект").Метаданные().ПолноеИмя() + ".Форма.";
    Версия = РеквизитФормыВЗначение("Объект").ПолучитьВерсию();
    ЕХТ_Чат_ПриСозданииНаСервере(Отказ, СтандартнаяОбработка);
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
    // Инициализация модулей — DI через Инициализация(Владелец)
    МодульПарсер = ПолучитьФорму(ПутьКФормам + "МодульПарсер", , ЭтаФорма);
    МодульMCP    = ПолучитьФорму(ПутьКФормам + "МодульMCP",    , ЭтаФорма);
    МодульMCP.Инициализация(ЭтаФорма);
    МодульBridge = ПолучитьФорму(ПутьКФормам + "МодульBridge",  , ЭтаФорма);
    МодульBridge.Инициализация(ЭтаФорма);
КонецПроцедуры

// === Центральный диспетчер событий от модулей ===
&НаКлиенте
Процедура ОбработатьСобытиеМодуля(ИмяМодуля, Событие, Данные) Экспорт
    // Bridge → Парсер → обработка по типу → UI
    Если Событие = "bridge_message" Тогда
        ОбработатьСообщениеBridge(Данные);
    ИначеЕсли Событие = "bridge_connected" Тогда
        ВывестиВЧатИнформацию("Подключено к Bridge");
    // ... другие события ...
    КонецЕсли;
КонецПроцедуры

// === Каскадное отключение ===
&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы)
    Если ЗавершениеРаботы Тогда Возврат; КонецЕсли;
    Если МодульBridge <> Неопределено Тогда
        МодульBridge.Отключиться();
    КонецЕсли;
КонецПроцедуры
```

---

## 3. Шаблоны форм-модулей

### 3.1 МодульBridge

```1c
// МодульBridge/Ext/Form/Module.bsl

&НаКлиенте
Перем ГлавнаяФорма;
&НаКлиенте
Перем МодульПарсер;
&НаКлиенте
Перем МодульMCP;
&НаКлиенте
Перем Соединение;  // WebSocket соединение

// --- Контракт модуля ---

&НаКлиенте
Процедура Инициализация(ВладелецФормы, Парсер, MCP) Экспорт
    ГлавнаяФорма = ВладелецФормы;
    МодульПарсер = Парсер;
    МодульMCP = MCP;
КонецПроцедуры

&НаКлиенте
Функция ОтключениеМодуля() Экспорт
    ОтключитьсяОтBridge();
    ГлавнаяФорма = Неопределено;
    МодульПарсер = Неопределено;
    МодульMCP = Неопределено;
КонецФункции

// --- Публичный API ---

&НаКлиенте
Процедура ПодключитьсяКBridge() Экспорт
    // ... логика WebSocket подключения ...
КонецПроцедуры

&НаКлиенте
Процедура ОтключитьсяОтBridge() Экспорт
    Если Соединение <> Неопределено Тогда
        Попытка Соединение.Закрыть(); Исключение КонецПопытки;
        Соединение = Неопределено;
    КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьВClaude(ТекстСообщения) Экспорт
    // ... формирование JSON и отправка ...
КонецПроцедуры

// --- Обработчики WebSocket ---

&НаКлиенте
Процедура WS_ПриПолученииСообщения(Соединение, Сообщение) Экспорт
    // Делегирование парсеру
    МодульПарсер.ОбработатьОтветBridge(Соединение, Сообщение);
КонецПроцедуры
```

**Реализованный API (174 строки):**
- `Инициализация(Владелец)` — сохранение ссылки на главную форму
- `Подключиться(URL, SessionID, Модель)` — создание WebSocket с заголовками
- `Отключиться()` — закрытие соединения
- `ОтправитьСообщение(ТекстJSON)` — отправка через WebSocket
- `WS_ПриОткрытии/ПриЗакрытии/ПриОшибке/ПриПолученииСообщения` — обработчики
- Обратная связь через `Владелец.ОбработатьСобытиеМодуля("Bridge", событие, данные)`

### 3.2 МодульПарсер

```1c
// МодульПарсер/Ext/Form/Module.bsl

&НаКлиенте
Перем ГлавнаяФорма;

&НаКлиенте
Процедура Инициализация(ВладелецФормы) Экспорт
    ГлавнаяФорма = ВладелецФормы;
КонецПроцедуры

&НаКлиенте
Функция ОтключениеМодуля() Экспорт
    ГлавнаяФорма = Неопределено;
КонецФункции

// --- Публичный API ---

&НаКлиенте
Процедура ОбработатьОтветBridge(Соединение, СтрокаJSON) Экспорт
    // Парсинг через ЧтениеJSON в Соответствие (паттерн Vanessa — JSON callback)
    ЧтениеJSON = Новый ЧтениеJSON;
    ЧтениеJSON.УстановитьСтроку(СтрокаJSON);
    Данные = ПрочитатьJSON(ЧтениеJSON, Истина);  // Истина = Соответствие

    ТипСобытия = Данные["type"];
    Если ТипСобытия = "stream_event" Тогда
        ОбработатьStreamEvent(Данные);
    ИначеЕсли ТипСобытия = "result" Тогда
        ОбработатьResult(Данные);
    ИначеЕсли ТипСобытия = "mcp" Тогда
        // Делегирование MCP-модулю через главную форму
        ГлавнаяФорма.МодульMCP.ОбработатьMCPЗапрос(Соединение, Данные);
    ИначеЕсли ТипСобытия = "error" Тогда
        ОбработатьОшибку(Данные);
    КонецЕсли;
КонецПроцедуры
```

**Реализованный API (186 строк):**
- `РазобратьJSON(СтрокаJSON)` — парсинг через `ЧтениеJSON` + `ПрочитатьJSON(, Истина)` в Соответствие
- `ИзвлечьТекстДельты(Данные)` — извлечение `event.delta.text` из разобранного объекта
- `ИзвлечьТипСобытия(Данные)` — получение `type`
- `ИзвлечьЗначение(Данные, ИмяПоля)` — универсальный доступ к полям
- `СобратьJSON(Значение)` — сериализация через `ЗаписьJSON`
- `ЭкранироватьJSON(Текст)` — экранирование спецсимволов
- `ЗначениеДляJSON(Значение)` — конвертация 1С-значений

### 3.3 МодульMCP

```1c
// МодульMCP/Ext/Form/Module.bsl

&НаКлиенте
Перем ГлавнаяФорма;
&НаКлиенте
Перем ОбработчикиИнструментов;  // Соответствие: имя → имя метода

&НаКлиенте
Процедура Инициализация(ВладелецФормы) Экспорт
    ГлавнаяФорма = ВладелецФормы;

    // Реестр инструментов (паттерн СБИС — Strategy по имени)
    ОбработчикиИнструментов = Новый Соответствие;
    ОбработчикиИнструментов.Вставить("tools.database.query",  "Инструмент_Query");
    ОбработчикиИнструментов.Вставить("tools.metadata.get",    "Инструмент_Metadata");
    ОбработчикиИнструментов.Вставить("tools.code.eval",       "Инструмент_Eval");
    ОбработчикиИнструментов.Вставить("tools.code.exec",       "Инструмент_Exec");
КонецПроцедуры

&НаКлиенте
Функция ОтключениеМодуля() Экспорт
    ГлавнаяФорма = Неопределено;
    ОбработчикиИнструментов = Неопределено;
КонецФункции

// --- Публичный API ---

&НаКлиенте
Процедура ОбработатьMCPЗапрос(Соединение, ДанныеЗапроса) Экспорт
    Метод = ДанныеЗапроса["method"];
    ИДЗапроса = ДанныеЗапроса["request_id"];
    Параметры = ДанныеЗапроса["params"];

    // Strategy-dispatch по реестру (паттерн СБИС)
    ИмяОбработчика = ОбработчикиИнструментов.Получить(Метод);
    Если ИмяОбработчика = Неопределено Тогда
        ОтправитьMCPОшибку(Соединение, ИДЗапроса, "Неизвестный метод: " + Метод);
        Возврат;
    КонецЕсли;

    // Вызов конкретного обработчика
    Результат = Неопределено;
    Попытка
        // Каждый инструмент — отдельная процедура
        Если ИмяОбработчика = "Инструмент_Query" Тогда
            Результат = Инструмент_Query(Параметры);
        ИначеЕсли ИмяОбработчика = "Инструмент_Metadata" Тогда
            Результат = Инструмент_Metadata(Параметры);
        // ... аналогично для остальных
        КонецЕсли;
    Исключение
        ОтправитьMCPОшибку(Соединение, ИДЗапроса, ОписаниеОшибки());
        Возврат;
    КонецПопытки;

    ОтправитьMCPРезультат(Соединение, ИДЗапроса, Результат);
КонецПроцедуры

// --- Серверные инструменты ---

&НаСервере
Функция Инструмент_Query(Параметры)
    // ... ВыполнитьЗапросНаСервере ...
КонецФункции
```

**Реализованный API (335 строк):**
- `Инициализация()` — регистрация обработчиков в `Соответствие`
- `ЗарегистрироватьОбработчик(Имя, ИмяМетода)` — расширяемый реестр
- `ОбработатьЗапрос(Данные)` — диспетч по имени инструмента
- `СформироватьОтвет(RequestId, Результат, Ошибка)` — единообразный JSON-ответ
- Обработчики (серверные): `Обработчик_1c_query`, `Обработчик_1c_metadata`, `Обработчик_1c_eval`, `Обработчик_1c_exec`

### 3.4 МодульЧат (не реализован — рекомендация)

> **Статус:** не создан. Код ЕХТ_Чат остался в главной форме, т.к. тесно привязан к элементам формы. Ниже — шаблон для будущей реализации.

```1c
// МодульЧат/Ext/Form/Module.bsl

&НаКлиенте
Перем ГлавнаяФорма;
&НаКлиенте
Перем ТекущееСообщениеИД;     // ИД текущего streaming-сообщения
&НаКлиенте
Перем ТекущееСообщениеТекст;  // накопленный текст

&НаКлиенте
Процедура Инициализация(ВладелецФормы) Экспорт
    ГлавнаяФорма = ВладелецФормы;
    ТекущееСообщениеИД = "";
    ТекущееСообщениеТекст = "";
КонецПроцедуры

&НаКлиенте
Функция ОтключениеМодуля() Экспорт
    ГлавнаяФорма = Неопределено;
КонецФункции

// --- Публичный API ---

&НаКлиенте
Процедура ВывестиСообщениеПользователя(Текст) Экспорт
    // ... вызов ЕХТ_Чат через ГлавнаяФорма ...
КонецПроцедуры

&НаКлиенте
Процедура ВывестиСообщениеАссистента(Текст) Экспорт
    // ... вызов ЕХТ_Чат ...
КонецПроцедуры

// Стриминг: обновление текущего сообщения
&НаКлиенте
Процедура ОбработатьДельту(ТекстДельты) Экспорт
    Если ТекущееСообщениеИД = "" Тогда
        // Первая дельта — создаём пустой пузырь
        Сообщение = ЕХТ_Чат_НовоеСообщение("...", "Ассистент");
        ТекущееСообщениеИД = Сообщение.ИД;
        ЕХТ_Чат_ДобавитьСообщение(ГлавнаяФорма.Элементы.Чат1, Сообщение);
    КонецЕсли;
    // Накапливаем и обновляем
    ТекущееСообщениеТекст = ТекущееСообщениеТекст + ТекстДельты;
    СообщениеОбн = ЕХТ_Чат_НовоеСообщение(ТекущееСообщениеТекст, "Ассистент");
    СообщениеОбн.ИД = ТекущееСообщениеИД;
    ЕХТ_Чат_ОбновитьСообщение(ГлавнаяФорма.Элементы.Чат1, СообщениеОбн);
КонецПроцедуры

&НаКлиенте
Процедура ЗавершитьСтриминг() Экспорт
    ТекущееСообщениеИД = "";
    ТекущееСообщениеТекст = "";
КонецПроцедуры
```

---

## 4. Жизненный цикл и инициализация

### 4.1 Разделение серверной и клиентской инициализации

**Паттерн из Vanessa:** `ПриСозданииНаСервере` — серверные данные, `ПриОткрытии` — клиентские модули.

```1c
// Сервер: загрузка данных, макетов, настроек
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    ПутьКФормам = РеквизитФормыВЗначение("Объект").Метаданные().ПолноеИмя() + ".Форма.";
    ЕХТ_Чат_ПриСозданииНаСервере(Отказ, СтандартнаяОбработка);
    // Загрузка настроек из ObjectModule (паттерн Диадока)
    Объект = РеквизитФормыВЗначение("Объект");
    НастройкиПодключения = Объект.НастройкиЗагрузить("НастройкиПодключения");
КонецПроцедуры

// Клиент: создание модулей (паттерн Vanessa)
&НаКлиенте
Процедура ПриОткрытии(Отказ)
    ПутьКФормамКлиент = ПутьКФормам;
    ИнициализироватьМодули();
    // Автоподключение если были настройки
    Если ЗначениеЗаполнено(АдресBridge) Тогда
        МодульBridge.ПодключитьсяКBridge();
    КонецЕсли;
КонецПроцедуры
```

### 4.2 Каскадное отключение

**Паттерн из Vanessa:** массив `МодулиДляОтключения`, обход с `Попытка/Исключение`.

```1c
&НаКлиенте
Функция МассивМодулей()
    Результат = Новый Массив;
    Результат.Добавить(МодульBridge);
    Результат.Добавить(МодульПарсер);
    Результат.Добавить(МодульMCP);
    Возврат Результат;
КонецФункции

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы)
    Если ЗавершениеРаботы Тогда Возврат; КонецЕсли;
    Для Каждого Модуль Из МассивМодулей() Цикл
        Если Модуль <> Неопределено Тогда
            Попытка Модуль.ОтключениеМодуля(); Исключение КонецПопытки;
        КонецЕсли;
    КонецЦикла;
КонецПроцедуры
```

### 4.3 Временное состояние — в переменных, не в реквизитах

**Паттерн из Vanessa:** переменные формы `Перем` для временного состояния, реквизиты формы — только для данных, которые нужны серверу или которые сохраняются.

```1c
// Временное состояние — Перем (не сохраняется, не передаётся на сервер)
&НаКлиенте
Перем Занят;
&НаКлиенте
Перем МожноЗакрытьФорму;

// Настройки — реквизиты формы (сохраняются через AutoSave)
// Модель, АдресBridge, Чат1_Настройка_* — остаются реквизитами
```

---

## 5. Ограничения платформы

Обнаружены при реализации Lyra-Chat-multiform.epf. Это не причины отказываться от паттерна форм-модулей — это ограничения, которые нужно учитывать.

### 5.1 `&НаКлиентеНаСервереБезКонтекста` невидима через `ПолучитьФорму()`

Методы с директивой `&НаКлиентеНаСервереБезКонтекста` **недоступны** при вызове через `ПолучитьФорму()`. Платформа не находит такие методы: ошибка `Метод объекта не обнаружен (ИмяМетода)`.

Через `ПолучитьФорму()` видны только методы с директивами `&НаКлиенте` и `&НаСервере`.

**Правило:** все экспортные методы форм-модулей, вызываемые из других форм, должны быть `&НаКлиенте`. Директива `&НаКлиентеНаСервереБезКонтекста` работает только для вызовов **внутри той же формы**.

```1c
// НЕПРАВИЛЬНО — не будет видна через ПолучитьФорму():
&НаКлиентеНаСервереБезКонтекста
Функция РазобратьJSON(СтрокаJSON) Экспорт  // ← ошибка при вызове из другой формы

// ПРАВИЛЬНО:
&НаКлиенте
Функция РазобратьJSON(Знач СтрокаJSON) Экспорт  // ← работает
```

**Совет:** если метод изначально был `&НаКлиентеНаСервереБезКонтекста`, можно оставить комментарий для документирования:
```1c
//*-+ &НаКлиентеНаСервереБезКонтекста
&НаКлиенте
Функция РазобратьJSON(Знач СтрокаJSON) Экспорт
```

### 5.2 ISP-нарушение форм-модулей

Форма-модуль формально нарушает принцип разделения интерфейсов (Interface Segregation Principle) — вызывающий код получает весь интерфейс `УправляемаяФорма` (сотни свойств и методов), хотя использует лишь 3–5 экспортных методов модуля.

**Однако в EPF это единственный механизм модуляризации**, и при соблюдении конвенций (минимальный экспортный интерфейс, документирование публичных методов) он надёжно работает. Альтернативы — макеты с `Выполнить()` — значительно хуже: теряется автодополнение, статический анализ и навигация по коду.

### 5.3 Клиент-серверный «пинг-понг»

При вызове `&НаСервере`-метода формы-модуля через `ПолучитьФорму()` происходит дополнительный клиент-серверный переход. Если главная форма уже на сервере и вызывает серверный метод модуля — получается цепочка клиент → сервер (главная) → клиент → сервер (модуль).

**Решение:** для межформенных вызовов использовать `&НаКлиенте`-обёртки, а серверную логику инкапсулировать внутри формы-модуля. Так сделано в МодульMCP: экспортные методы `&НаКлиенте`, серверные обработчики — внутренние `&НаСервере`.

---

## 6. WebSocket и Bridge

### 6.1 Retry с прогрессивной задержкой

**Паттерн из Vanessa (retry файловых операций):** при ошибке — повторять с увеличивающейся задержкой.

```1c
// МодульBridge
&НаКлиенте
Перем ПопытокПереподключения;

&НаКлиенте
Процедура WS_ПриОшибке(Соединение, КодОшибки, Описание) Экспорт
    ПопытокПереподключения = ПопытокПереподключения + 1;
    Если ПопытокПереподключения <= 3 Тогда
        ЗадержкаСек = ПопытокПереподключения * 2;  // 2, 4, 6 сек
        ПодключитьОбработчикОжидания("ПереподключитьсяКBridge", ЗадержкаСек, Истина);
    Иначе
        Владелец.ОбработатьСобытиеМодуля("Bridge", "bridge_error",
            "Не удалось подключиться к Bridge после 3 попыток");
    КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура WS_ПриОткрытии(Соединение) Экспорт
    ПопытокПереподключения = 0;  // сбросить при успехе
    Владелец.ОбработатьСобытиеМодуля("Bridge", "bridge_connected", "");
КонецПроцедуры
```

### 6.2 Адрес Bridge — из настроек

**Паттерн из Диадока (функциональные опции):** конфигурируемые параметры — в реквизитах формы с AutoSave, не в хардкоде.

```1c
// Вместо хардкода "ws://localhost:3003"
&НаКлиенте
Процедура ПодключитьсяКBridge() Экспорт
    Адрес = ГлавнаяФорма.АдресBridge;
    Если НЕ ЗначениеЗаполнено(Адрес) Тогда
        Адрес = "ws://localhost:3003";  // значение по умолчанию
    КонецЕсли;
    // ... подключение ...
КонецПроцедуры
```

---

## 7. Парсинг JSON

### 7.1 ✅ Замена строкового парсера на ЧтениеJSON

**Реализовано в МодульПарсер.** Строковый парсер (4 хрупкие функции) заменён на `ЧтениеJSON` + `ПрочитатьJSON(, Истина)` → Соответствие.

**Реализация:**

```1c
// МодульПарсер — одна функция вместо четырёх
&НаКлиенте
Функция РазобратьJSON(СтрокаJSON)
    ЧтениеJSON = Новый ЧтениеJSON;
    ЧтениеJSON.УстановитьСтроку(СтрокаJSON);
    // Истина = парсить в Соответствие (ключи с дефисами — ok)
    Возврат ПрочитатьJSON(ЧтениеJSON, Истина);
КонецФункции

// Использование:
Данные = РазобратьJSON(СтрокаJSON);
ТипСобытия = Данные["type"];
// Для stream_event — безопасный доступ через вложенные ключи:
Событие = Данные.Получить("event");
Если Событие <> Неопределено Тогда
    Дельта = Событие.Получить("delta");
    Если Дельта <> Неопределено Тогда
        ТекстДельты = Дельта.Получить("text");
    КонецЕсли;
КонецЕсли;
```

### 7.2 ✅ Сборка JSON через структуру

**Реализовано в МодульПарсер** (`СобратьJSON`). Структура/Соответствие сериализуется через `ЗаписьJSON`.

```1c
// Вместо ручной конкатенации:
// ОтветJSON = "{""type"":""mcp_response"",""requestId"":""" + ИД + """,...}"

// Правильно:
Ответ = Новый Структура;
Ответ.Вставить("type", "mcp_response");
Ответ.Вставить("request_id", ИДЗапроса);
Ответ.Вставить("result", Результат);

ЗаписьJSON = Новый ЗаписьJSON;
ЗаписьJSON.УстановитьСтроку();
ЗаписатьJSON(ЗаписьJSON, Ответ);
ОтветJSON = ЗаписьJSON.Закрыть();
```

---

## 8. MCP-инструменты

### 8.1 ✅ Strategy-dispatch по реестру

**Реализовано в МодульMCP.** Реестр обработчиков вместо длинного `Если/ИначеЕсли`. Новый инструмент — одна строка в реестре.

```1c
// Регистрация — одна строка на инструмент
ОбработчикиИнструментов.Вставить("tools.database.query",  "Инструмент_Query");
ОбработчикиИнструментов.Вставить("tools.metadata.get",    "Инструмент_Metadata");
// Добавление нового:
ОбработчикиИнструментов.Вставить("tools.document.post",   "Инструмент_PostDocument");
```

### 8.2 ✅ Единообразный формат ответа

**Реализовано в МодульMCP** (`СформироватьОтвет`).

```1c
// Все инструменты возвращают одинаковую структуру
Функция НовыйРезультатMCP(Успех, Данные, ТекстОшибки = "")
    Результат = Новый Структура;
    Если Успех Тогда
        Результат.Вставить("result", Данные);
    Иначе
        Результат.Вставить("error", ТекстОшибки);
    КонецЕсли;
    Возврат Результат;
КонецФункции
```

---

## 9. Кэширование и состояние

### 9.1 Кэш в переменных модуля

**Паттерн из Vanessa (кэш в переменных формы):** `Неопределено` = «не вычислено».

```1c
// МодульMCP — кэш метаданных
&НаКлиенте
Перем КэшМетаданных;  // Неопределено = не загружено

&НаКлиенте
Функция ПолучитьМетаданные()
    Если КэшМетаданных = Неопределено Тогда
        КэшМетаданных = ПолучитьМетаданныеНаСервере();
    КонецЕсли;
    Возврат КэшМетаданных;
КонецФункции
```

### 9.2 Отделение сохраняемых настроек от временного состояния

| Тип данных | Хранение | Примеры в Lyra-Chat |
|---|---|---|
| Настройки пользователя | Реквизиты формы + AutoSave | `Модель`, `АдресBridge`, `Чат1_Настройка_*` |
| Временное состояние | `Перем` в модуле | `Занят`, `МожноЗакрытьФорму`, `ТекущееСообщениеТекст` |
| Кэши | `Перем` в модуле | `КэшМетаданных`, `КэшФормПлагинов` |
| История диалогов | `ХранилищеОбщихНастроек` | Массив `{роль, текст, время}` |

---

## 10. Настройки и хранилища

### 10.1 ✅ Обёртки в ObjectModule

**Паттерн из Диадока (lazy-init кэш + обёртки ЖР):**

```1c
// ObjectModule.bsl

Функция НастройкиЗагрузить(КлючНастроек, ЗначениеПоУмолчанию = Неопределено) Экспорт
    Попытка
        Результат = ХранилищеОбщихНастроек.Загрузить("LyraChat", КлючНастроек);
    Исключение
        Результат = Неопределено;
    КонецПопытки;
    Если Результат = Неопределено Тогда
        Результат = ЗначениеПоУмолчанию;
    КонецЕсли;
    Возврат Результат;
КонецФункции

Процедура НастройкиСохранить(КлючНастроек, Значение) Экспорт
    Попытка
        ХранилищеОбщихНастроек.Сохранить("LyraChat", КлючНастроек, Значение);
    Исключение
        // Безопасный режим или нет прав
    КонецПопытки;
КонецПроцедуры
```

### 10.2 Структура ключей

```
"LyraChat" → "НастройкиПодключения"   // {АдресBridge, Модель, СистемныйПромпт}
"LyraChat" → "НастройкиUI"            // {Тема, Аватары, Автосвертка}
"LyraChat" → "СписокСессий"           // [{ИД, Название, Дата}]
"LyraChat" → "История/" + SessionID   // [{Роль, Текст, Время}]
```

### 10.3 Неизменяемые конфигурации

**Паттерн из Диадока (ФиксированнаяСтруктура):**

```1c
// Настройки MCP-инструментов — нельзя случайно изменить
Функция НастройкиMCP() Экспорт
    Н = Новый Структура;
    Н.Вставить("МаксДлинаРезультата", 100000);
    Н.Вставить("ТаймаутЗапроса", 30);
    Н.Вставить("РазрешитьExec", Ложь);  // по умолчанию — запретить
    Возврат Новый ФиксированнаяСтруктура(Н);
КонецФункции
```

---

## 11. Обработка ошибок

### 11.1 ЖР с префиксом

**Паттерн из Диадока (обёртки ЖР с «Диадок.» префиксом):**

```1c
// ObjectModule.bsl
Процедура ЖР_Ошибка(ВидОперации, Комментарий) Экспорт
    ЗаписьЖурналаРегистрации(
        "LyraChat." + ВидОперации,
        УровеньЖурналаРегистрации.Ошибка, , , Комментарий,
        РежимТранзакцииЗаписиЖурналаРегистрации.Независимая);
КонецПроцедуры

Процедура ЖР_Информация(ВидОперации, Комментарий) Экспорт
    ЗаписьЖурналаРегистрации(
        "LyraChat." + ВидОперации,
        УровеньЖурналаРегистрации.Информация, , , Комментарий);
КонецПроцедуры
```

Использование:
```1c
// Из МодульMCP:
ЖР_Ошибка("MCP.Query", "Ошибка запроса: " + ОписаниеОшибки());
// В ЖР появится: "LyraChat.MCP.Query" — легко фильтровать
```

### 11.2 Централизованный обработчик ошибок

**Паттерн из Диадока (Ядро.Ошибка_Обработать):**

```1c
// МодульЧат — единая точка отображения ошибок
&НаКлиенте
Процедура ОбработатьОшибку(ВидОперации, ОписаниеОшибки) Экспорт
    // 1. Показать пользователю
    ВывестиОшибку(ВидОперации + ": " + ОписаниеОшибки);
    // 2. Записать в ЖР (через серверный вызов)
    ЗаписатьОшибкуНаСервере(ВидОперации, ОписаниеОшибки);
КонецПроцедуры
```

### 11.3 Отложенная очистка ресурсов

**Паттерн из Диадока (ВременныеФайлыКУдалению):**

```1c
// Если Lyra-Chat создаёт временные файлы (экспорт, скриншоты):
&НаКлиенте
Перем ВременныеФайлыКУдалению;

&НаКлиенте
Процедура ДобавитьВременныйФайл(Путь)
    Если ВременныеФайлыКУдалению = Неопределено Тогда
        ВременныеФайлыКУдалению = Новый Массив;
    КонецЕсли;
    ВременныеФайлыКУдалению.Добавить(Путь);
КонецПроцедуры

// В ПередЗакрытием:
Если ВременныеФайлыКУдалению <> Неопределено Тогда
    Для Каждого Файл Из ВременныеФайлыКУдалению Цикл
        Попытка УдалитьФайлы(Файл); Исключение КонецПопытки;
    КонецЦикла;
КонецЕсли;
```

---

## 12. UI и динамические формы

### 12.1 Форма-прогресс с API

**Паттерн из Диадока (ИндикацияПрогрессаОбработкиДанныхУправляемая):** если Lyra будет выполнять длительные MCP-операции — отдельная форма-виджет.

```1c
// Использование:
Прогресс = ПолучитьФорму(ПутьКФормамКлиент + "ФормаПрогресс", , ЭтаФорма);
Прогресс.Показать("Выполнение запроса...");
// ... длительная операция ...
Прогресс.Скрыть();
```

### 12.2 ✅ ФормаПодключение — настройки

**Реализовано.** ФормаПодключение содержит поля URL, модель, системный промпт. Возвращает параметры через `Закрыть(Результат)`.

```1c
// ФормаПодключение — открывается через ОткрытьФорму с ОписаниемОповещения
ПараметрыФормы = Новый Структура;
ПараметрыФормы.Вставить("АдресBridge", АдресBridge);
ПараметрыФормы.Вставить("Модель", Модель);
ПараметрыФормы.Вставить("СистемныйПромпт", СистемныйПромпт);

ОткрытьФорму(ПутьКФормамКлиент + "ФормаПодключение",
    ПараметрыФормы, ЭтаФорма, , , ,
    Новый ОписаниеОповещения("ОбработкаЗакрытияНастроек", ЭтаФорма));

&НаКлиенте
Процедура ОбработкаЗакрытияНастроек(Результат, ДопПараметры) Экспорт
    Если Результат = Неопределено Тогда Возврат; КонецЕсли;
    АдресBridge = Результат.АдресBridge;
    Модель = Результат.Модель;
    // Переподключиться с новыми настройками
    МодульBridge.ОтключитьсяОтBridge();
    МодульBridge.ПодключитьсяКBridge();
КонецПроцедуры
```

---

## 13. Совместимость

### 13.1 ✅ Динамическое имя обработки

**Реализовано в ObjectModule** (`ПолноеИмя()` через `ЭтотОбъект.Метаданные().ПолноеИмя()`).

```1c
// Вместо хардкода "ВнешняяОбработка.ЕХТ_Чат_БезРасширения"
&НаСервере
Функция ПолучитьПутьКФормам()
    Возврат РеквизитФормыВЗначение("Объект").Метаданные().ПолноеИмя() + ".Форма.";
КонецФункции
```

### 13.2 Условная компиляция (на будущее)

**Паттерн из Vanessa:** если Lyra-Chat когда-нибудь будет работать в веб-клиенте — WebSocket там недоступен.

```1c
&НаКлиенте
Процедура ПодключитьсяКBridge()
    #Если ВебКлиент Тогда
    ВывестиОшибку("WebSocket недоступен в веб-клиенте");
    Возврат;
    #КонецЕсли
    // ... основная логика ...
КонецПроцедуры
```

---

## 14. Тестируемость

### 14.1 Модули как точки тестирования

Разбиение на модули автоматически улучшает тестируемость — каждый модуль можно тестировать изолированно:

```1c
// Тест парсера — без WebSocket и UI
МодульПарсер = ПолучитьФорму("...МодульПарсер", , ЭтаФорма);
МодульПарсер.Инициализация(ЭтаФорма);

ТестJSON = "{""type"":""stream_event"",""event"":{""delta"":{""text"":""Привет""}}}";
Результат = МодульПарсер.РазобратьJSON(ТестJSON);
// Проверить: Результат["type"] = "stream_event"
```

### 14.2 Транзакция-песочница для MCP

**Паттерн из Vanessa (ПроверитьЗагрузкуМакета):** для тестирования MCP-инструментов, которые модифицируют данные.

```1c
// Тестовый режим: все запросы в транзакции с откатом
&НаСервере
Функция Инструмент_Exec_Тест(Код)
    НачатьТранзакцию();
    Попытка
        Выполнить(Код);
        ОтменитьТранзакцию();  // всегда откат
        Возврат НовыйРезультатMCP(Истина, "OK (тестовый режим)");
    Исключение
        ОтменитьТранзакцию();
        Возврат НовыйРезультатMCP(Ложь, , ОписаниеОшибки());
    КонецПопытки;
КонецФункции
```

---

## 15. Статус реализации

### Выполнено

| # | Задача | Источник паттерна |
|---|---|---|
| ✅ | Динамический `ПолноеИмя()` через метаданные | Диадок: рефлексия |
| ✅ | Парсинг JSON через `ЧтениеJSON` + Соответствие (МодульПарсер) | Vanessa: JSON callback |
| ✅ | Сборка JSON через `ЗаписьJSON` (МодульПарсер) | Vanessa |
| ✅ | Strategy-dispatch для MCP-инструментов (МодульMCP) | СБИС: Strategy |
| ✅ | WebSocket вынесен в МодульBridge | Vanessa: модули |
| ✅ | Обёртки настроек в ObjectModule | Диадок: lazy-init кэш |
| ✅ | ФормаПодключение — URL, модель, промпт | — |
| ✅ | DI через `Инициализация(Владелец)` | Vanessa + видео-разбор |
| ✅ | События через `ОбработатьСобытиеМодуля()` | Vanessa: Owner Chain |
| ✅ | Экспортные методы `&НаКлиенте` (не `&НаКлиентеНаСервереБезКонтекста`) | Обнаружено при реализации |

### Не реализовано (следующие шаги)

| # | Задача | Сложность | Приоритет |
|---|---|---|---|
| 1 | Стриминг — обновление пузыря через `ЕХТ_Чат_ОбновитьСообщение` | Малая | Высокий |
| 2 | МодульЧат — вынести вывод из главной формы | Малая | Средний |
| 3 | Автопереподключение с progressive backoff | Малая | Средний |
| 4 | Обёртки ЖР с префиксом «LyraChat.» | Малая | Средний |
| 5 | История диалогов в `ХранилищеОбщихНастроек` | Средняя | Низкий |
| 6 | Мультисессионность (несколько диалогов) | Высокая | Низкий |

### Обнаруженные ограничения платформы

Подробно описаны в разделе 5. Краткая сводка:

1. **`&НаКлиентеНаСервереБезКонтекста` невидима через `ПолучитьФорму()`** — все экспортные методы модулей должны быть `&НаКлиенте`
2. **ISP-нарушение** — форма-модуль экспонирует весь интерфейс `УправляемаяФорма`, но альтернатив в EPF нет
3. **Клиент-серверный пинг-понг** — серверные методы формы-модуля вызываются с дополнительным переходом, решается `&НаКлиенте`-обёртками

---

## 16. Как использовать

1. Открыть `src/` в конфигураторе 1С через «Загрузить внешнюю обработку из файлов»
2. Проверить что все 5 форм отображаются в дереве объектов
3. Проверить компиляцию модулей (Ctrl+F7)
4. Запустить обработку — проверить инициализацию модулей в `ПриОткрытии`
5. Подключиться к Bridge — проверить WebSocket через МодульBridge
6. Отправить сообщение — проверить цепочку Форма → Bridge → Парсер → UI
7. Проверить MCP-запрос — проверить диспетч через МодульMCP